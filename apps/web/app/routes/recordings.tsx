export function RecordingsPage() {
  return (
    <section className="flex min-h-0 flex-1 flex-col overflow-y-auto px-8 pt-7 pb-10">
      <h1 className="text-2xl font-semibold tracking-[-0.02em]">Recordings</h1>
      <p className="mt-2 text-sm text-[var(--mr-mfg)]">
        Recordings list is not implemented yet. Linked from project cards until Workspace ships.
      </p>
    </section>
  );
}
